<%= form_with(model: parking_space) do |form| %>
  <% if parking_space.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(parking_space.errors.count, "error") %> prohibited this parking_space from being saved:</h2>

      <ul>
        <% parking_space.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :entrance_id, style: "display: block" %>
    <%= form.collection_select :entrance_id, Entrance.all, :id, :name %>
  </div>

  <div>
    <%= form.label :space_size %>
    <%= form.collection_select :space_size, ParkingSpace::SIZE_OPTIONS, :last, :first %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
